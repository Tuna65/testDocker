import React from "react";

interface Props {}

const Login = (props: Props) => {
  const {} = props;
  React.useEffect(() => {}, []);
  return <div className="flex text-3xl font-bold underline">hello</div>;
};

export default Login;
